# 数据准备手册

本手册用于指导 V1 训练数据的采集、整理、标注复核和 manifest 元数据填写。主方案只保留 gate 摘要；具体数据准备细则以本手册为准。

执行者必须先按本手册准备数据，再进入主方案步骤 2。未达到正式 V1 最低要求时，只允许做小样本流程验证，不允许称为正式 V1 训练。

## 1. 数据范围

V1 面向材料/粉体颗粒显微图。正式数据集应优先覆盖材料、粉体、颗粒、团聚体、填料、纳米/微米颗粒等相关图像；不要把 V1 扩展成“所有图片都能分割”的通用方案。

| 优先级 | 图片类型 | manifest 字段 | 要求 |
| --- | --- | --- | --- |
| 建议记录 | SEM 扫描电镜颗粒图 | `microscope_type=SEM` | 作为来源/成像域记录和分层分析字段；不是模型推理或 C++ 后处理正确性的硬 gate。 |
| 建议记录 | TEM/TSEM 颗粒图 | `microscope_type=TEM` 或 `TSEM` | 能确认时记录；无法可靠确认时不要硬猜，可保留 `unknown` 并在 `source`/`notes` 写明来源线索。 |
| 可选记录 | 光学显微镜颗粒图 | `microscope_type=optical` | 数据充足时可作为实验支持；是否加入取决于目标业务域。 |
| 不建议 V1 使用 | 非材料/粉体颗粒图、宏观照片、无显微镜图、报告截图、带大量文字标注的图、带信息栏的未裁剪显微图 | 不进入训练数据集 | 这些图片会污染训练目标。 |

每张图必须能看清颗粒轮廓。训练、验证和 holdout 数据必须只包含颗粒显微视野区域，不得包含底部或侧边信息栏、比例尺文字、倍率文字、仪器参数、仪器 UI、报告文字、黑白边框或其它非显微视野内容。

如果原始完整图带信息栏，必须先裁剪出颗粒显微视野 ROI，再进入训练、验证或 holdout 数据集；对应标签必须基于裁剪后的 ROI，且尺寸与 ROI 图像完全一致。原始完整图可单独归档，用于追溯或结果回显；如需在原图上显示 mask，应由上层软件保存 ROI 偏移并把结果映射回原图坐标。

比例尺、倍率、粒径单位和报告统计信息不是模型输入，不需要放进训练 manifest。需要保留时应写入外部记录、recipe 或 Qt 应用层配置，不得混入训练图片。

实际推理时允许使用“主分析 ROI + ignore regions”两层机制：主分析 ROI 对齐训练图像域；ROI 内仍存在的标尺、标尺文字、其它文字、污染、遮挡等局部区域，由 Qt/C++ 运行时作为 ignore regions 屏蔽。ignore regions 属于运行时分析配置，不属于训练 manifest，也不要通过修改训练图片来实现。

## 2. 数量要求

正式 V1 训练不得只用几十张图。这里的“原始已标注完整图片”指进入主方案步骤 2 前已经有实例标签并完成人工复核的完整原图；“有效 train/val 样本”可以是整图，也可以是大图切出的 tile。

| 数据层级 | 最低数量 | 推荐数量 | 说明 |
| --- | ---: | ---: | --- |
| 原始已标注完整图片 | 150 张 | 300 张以上 | 正式 V1 的人工采集底线；大图切 tile 后会增加训练样本数。 |
| 有效 train 样本 | 200 个 | 500 个以上 | 由划分后的整图或 tile 组成。 |
| 有效 val 样本 | 30 个 | 80 个以上 | 必须覆盖主要场景，不能只来自单一来源。 |
| holdout 完整原图 | 10 张 | 30 张以上 | 不参与训练，只做最终轮廓验收；其中应至少包含 3 张大图。 |
| 大图源图 | 5 张 | 15 张以上 | 长边大于 1536；用于验证 tile 训练和 C++ tile 推理。 |

代码强制 gate 与人工采集目标需要区分：

- 代码会强制检查第 2 章中的 train、val、holdout、大图源图和 holdout 大图最低数量要求，以及 train/val 全部 `label_status=reviewed`、V1 必填场景字段（不含 `microscope_type`）非空且非 `unknown`、不得出现比例尺相关 manifest 字段。
- 代码会统计并报告 `microscope_type` 分布；如果 train/val 没有任何可靠显微镜类型或成像域记录，会给出 warning，但不阻断训练。
- 人工采集目标包括第 2 章的原始已标注完整图片数量、第 3 章的场景覆盖、来源/成像域分布记录。这些目标不会全部由脚本逐项硬校验，但正式 V1 训练前仍应人工复核并写入 readiness 记录。

## 3. 场景覆盖要求

准备样本时不要只收集“看起来最漂亮”的图。正式 V1 至少覆盖下面场景：

| 维度 | 必须覆盖 | 建议数量 |
| --- | --- | --- |
| 来源/成像域 | 尽量记录 `SEM`、`TEM`、`TSEM`、`optical` 或 `unknown` | 用于分层分析和诊断，不作为轮廓推理正确性的硬 gate；尽量覆盖主要业务来源。 |
| 颗粒形态 | `round_like`、`irregular`、`mixed` | 每类至少 20 张原始图；片状/纤维/多孔有实际需求时再单独补。 |
| 密度等级 | `sparse`、`medium`、`dense` | 每类至少 20 张原始图。 |
| 粘连程度 | `none`、`few_touching`、`many_touching` | `few_touching` 和 `many_touching` 合计不少于 30 张原始图。 |
| 图像质量 | `clear`、`low_contrast`、`blurred` 或 `noisy_texture` | 低质量真实样本不能全删，至少保留 20 张用于泛化。 |
| 大图 | `is_large_image=true` | 数量要求见第 2 章；用于验证 tile 训练和 C++ tile 推理。 |
| 视觉尺度分布 | `small`、`medium`、`large`、`wide` | 用于训练覆盖，不作为最终验收指标。 |

同一张图可以同时覆盖多个维度，例如“来源为 SEM + irregular + dense + many_touching + low_contrast + 大图”。

必须把关键组合场景作为单独覆盖目标复核，而不能只检查单个字段是否出现。`dense + small + round_like + none/few_touching` 是 V1 的重点难例组合，代表大量小颗粒彼此很近、形状接近圆形、但实际粘连很少的显微图。正式 V1 数据集中应包含足够数量的真实、人工复核样本覆盖该组合：训练集中要有这类样本供模型学习，val 中要有这类样本用于选择 checkpoint 和诊断，小规模调参集必须包含这类样本用于 FlowDynamics 对齐，holdout 中也应保留这类完整原图用于最终验收。不能把这类样本全部放入 train，也不能只用合成图替代 val 或 holdout 中的真实样本。

对 `dense + small + round_like + none/few_touching` 样本，标注复核要特别检查三点：第一，未粘连但距离很近的颗粒必须拆成不同实例；第二，小颗粒不能因为面积小、边界弱或对比低而漏标；第三，颗粒轮廓不要向外扩张到背景纹理。若图像四周存在边框、阴影、信息栏残留或非颗粒纹理，应先裁剪 ROI 或在运行时使用 ignore regions，不得把这些区域标成颗粒，也不得让它们进入训练标签。

场景覆盖表用于指导数据采集和人工复核。脚本会强制拦截必填场景字段的空值和 `unknown`，但不会因为缺少 `SEM`、`TEM` 或 `TSEM` 阻断训练；显微镜类型只用于分层划分、分层分析和诊断。这部分需要执行者在数据准备阶段主动记录并复核。

## 4. 元数据填写规则

主方案步骤 3 会生成 draft manifest，其中很多字段会先是 `unknown`。执行者必须把必填字段按下表改成准确值；`microscope_type` 是来源/成像域记录字段，无法可靠确认时可保留 `unknown`。

| 字段 | 是否必填 | 可选值 | 填写规则 |
| --- | --- | --- | --- |
| `microscope_type` | 建议填写 | `SEM` / `TEM` / `TSEM` / `optical` / `unknown` | 记录显微镜来源或成像域，用于分层分析和诊断。看图片来源或文件夹名；扫描电镜填 `SEM`，透射电镜填 `TEM`，透射扫描填 `TSEM`。不确定时不要猜，可保留 `unknown`，并在 `source` 或 `notes` 记录来源线索。 |
| `particle_morphology` | 是 | `round_like` / `irregular` / `flake_like` / `fiber_like` / `porous` / `mixed` | 看整张图主要形态。圆/椭圆居多填 `round_like`；边缘不规则填 `irregular`；片状填 `flake_like`；细长纤维填 `fiber_like`；多孔填 `porous`；多种混合且无主导填 `mixed`。 |
| `density_level` | 是 | `sparse` / `medium` / `dense` | 颗粒间距大、基本不接触填 `sparse`；有接近但多数可分开填 `medium`；大量接触、重叠或间距很小填 `dense`。 |
| `size_distribution` | 是 | `small` / `medium` / `large` / `wide` | 这是训练覆盖字段，不是最终粒径验收。肉眼看多数颗粒很小填 `small`，中等填 `medium`，明显大颗粒为主填 `large`，大小混杂明显填 `wide`。 |
| `quality_level` | 是 | `clear` / `low_contrast` / `blurred` / `noisy_texture` | 边界清晰填 `clear`；颗粒和背景灰度接近填 `low_contrast`；整体虚焦填 `blurred`；背景纹理或噪声明显填 `noisy_texture`。 |
| `adhesion_level` | 是 | `none` / `few_touching` / `many_touching` | 颗粒基本不接触填 `none`；少数接触填 `few_touching`；大量接触、粘连、重叠填 `many_touching`。 |
| `is_large_image` | 是 | `true` / `false` | 长边大于 1536 像素填 `true`，否则填 `false`。脚本会自动初填，但人工复核时要确认。 |
| `label_status` | 是 | `unlabelled` / `prelabelled` / `reviewed` / `unknown` | 只有人工检查过、确认实例边界和粘连拆分正确的样本才能填 `reviewed`。正式 train/val 必须是 `reviewed`。 |
| `split` | 暂不必手工填 | `unassigned` / `train` / `val` / `holdout` | 训练候选保持 `unassigned`，交给脚本划分；最终验收完整原图手工填 `holdout`。 |
| `notes` | 否 | 任意文字 | 记录特殊情况，例如“低对比但真实场景”“疑似杂质已排除”。 |

CSV 中还会出现一些脚本字段。`source`、`image_path`、`file_name`、`label_path` 由主方案步骤 3 生成；`flow_path` 由步骤 7 生成；`is_tile`、`source_image_path`、`tile_x`、`tile_y`、`tile_width`、`tile_height`、`tile_overlap`、`tile_core_margin`、`tile_role` 由步骤 5 生成；`shape_type` 是兼容旧字段，正式填写以 `particle_morphology` 为准；`polarity` 暂时可以保持 `unknown`。不要手工编造这些脚本字段。

`dataset_manifest.csv` 不需要比例尺相关字段，正式 manifest 中也不应存在这些列。不要创建、保留或填写 `has_scale`、`pixel_size`、`pixel_size_unit`、`scale_source`。如果 CSV 中出现这些列，manifest gate 会报错。比例尺属于下游分析软件，不属于模型训练数据。

## 5. 标注质量要求

每张 `*_labels.png` 必须满足：

- 空间尺寸（高度、宽度）必须与对应原图完全一致。主方案步骤 3 所有格式（generic、EMPS、mask_only 等）都会硬校验此项，不一致则拒绝进入 staging。
- 背景像素为 `0`。
- 每个颗粒实例使用唯一正整数 id。
- 轻微接触且人工能稳定判断分割边界的颗粒，必须拆成不同 id；严重粘连、重叠或堆叠导致边界只能靠猜的颗粒，不得强行拆分。
- 不属于颗粒的杂质不要标成颗粒。信息栏、比例尺文字、倍率文字、仪器参数、仪器 UI、报告文字和黑白边框不应出现在训练图中；若原始图包含这些内容，先裁剪 ROI，再标注或同步裁剪标签。
- 颗粒轮廓要尽量贴合真实边缘，不要故意扩大或缩小。
- 空 mask、全黑 mask、只有一个巨大错误区域的 mask 不得进入正式数据。
- 三通道彩色图（RGB/BGR）不得作为标签；标签必须是单通道 `uint16` PNG 实例标签图。

V1 的标注目标是“可用于粒径计算的完整单颗粒投影”。标注对象必须同时满足：

- 属于目标材料/粉体颗粒，而不是背景纹理、孔洞、裂纹、膜层、网格、污染物或非目标相。
- 颗粒边界能由人工稳定闭合，轮廓不是主要依赖猜测或经验推断。
- 颗粒投影形态相对完整，面积、等效圆直径、长短轴等粒径指标有物理意义。
- 尺寸高于可靠测量下限。建议直径少于 `8-10 px` 的颗粒不标注，除非项目另行定义了更高分辨率和更小粒径的可靠验收标准。

以下对象不得标注为有效颗粒：

- 肉眼不能分辨边界、边界主要由噪声或背景纹理决定的颗粒。
- 严重粘连、重叠、堆叠或团聚体内部边界不可判定的“疑似单颗粒”。
- 明显被遮挡、只露出一部分、被其它颗粒覆盖、被图像边界截断的颗粒。
- 过曝、欠曝、失焦、运动拖影、严重扫描条纹、充电伪影或污染斑导致边界失真的区域。
- 空心、破碎、塌陷、残片、碎屑等不属于本次业务统计目标的对象；如果业务明确要统计碎片，必须单独在数据说明中定义。
- 同一颗粒只出现局部高亮边缘、主体不可见，无法恢复完整投影轮廓的对象。
- 信息栏、比例尺、比例尺文字、仪器文字、箭头、框线、图例、彩色 overlay、报告截图或拼图元素上的任何区域。

如果一张图片中符合要求的完整颗粒太少，或者多数颗粒都属于上述“不标注对象”，该图不应进入正式 train/val；可放入人工复核、困难样本库或运行时失败案例库，但不得标为 `reviewed` 训练样本。

二值 mask 历史数据可以作为起点，但必须人工复核。连通分量自动生成的实例标签经常会把粘连颗粒合并成一个实例，不能直接当作 `reviewed`。

## 6. 原始目录整理规则

进入主方案步骤 2 前，应先建立一个原始已标注数据根目录，例如 `E:\Datasets\ParticleV1Raw` 或项目指定的数据根目录。根目录下每个子目录表示一个来源批次或一个数据集子集。

子目录名建议带显微镜后缀，便于脚本自动填写初始 `microscope_type`：

- `_sem` -> `SEM`
- `_tem` -> `TEM`
- `_tsem` -> `TSEM`
- `_optical` -> `optical`

推荐使用通用实例标签格式：

```text
<已标注数据根目录>/
  batch_001_sem/
    image_0001.png
    image_0001_labels.png
    image_0002.tif
    image_0002_labels.png
  batch_002_tem/
    sample_a.jpg
    sample_a_labels.png
```

通用格式要求：

- 图片扩展名支持 `.jpg`、`.jpeg`、`.png`、`.bmp`、`.tif`、`.tiff`。
- 标签文件名必须是 `<图片 stem>_labels.png`，这里的 `stem` 是不带扩展名的文件名。
- 示例：原图 `image_0001.tif` 的标签必须是 `image_0001_labels.png`。
- 不要命名为 `image_0001.tif_labels.png`；当前正式脚本不会把它当作 `image_0001.tif` 的标签。
- 标签必须是单通道 `uint16` PNG 实例 label 图：`0` 表示背景，`1..N` 表示不同颗粒实例。
- 粘连颗粒如果应作为不同实例，必须在标签中使用不同 id。
- 空标签、二义性标签、缺标签图片不得进入正式数据。

也可以放入以下历史格式，主方案步骤 3 的准备脚本会按子目录名关键词自动识别：

- 名称包含 `emps`：需要 `images/` 与 `segmaps/`。
- 名称包含 `NIST`：需要 `intensity_sets/` 与 `mask_sets/masks/`。
- 名称包含 `nNPipe`：需要 `nNPipe_resources/nNPipe_resources/experimental_images/<subset>/image` 与 `target`。
- 名称包含 `TiO2`：支持 `image/mask`、`images/masks` 或 `img/gt`。

使用二值 mask 历史格式时，脚本会用连通分量生成实例标签；这类样本后续必须人工复核，确认粘连颗粒没有被错误合并，才能把 `label_status` 改为 `reviewed`。

## 7. 外部数据集接入规则

外部数据集、论文配套数据和网络下载数据不得直接进入正式 train/val。应先作为 candidate 批次整理，完成标签语义核验、格式转换和人工抽样复核。外部数据源评估记录、泛化策略和合成数据策略见 [外部数据源与泛化策略.md](外部数据源与泛化策略.md)；本章只规定稳定接入规则。

接入外部数据集时必须做到：

- 先阅读数据集 README、论文说明、Data Availability 和官方转换/训练脚本，确认原图、标签和可视化文件的真实含义。
- 抽样检查原图、标签图和 overlay 是否空间对齐，确认标签覆盖的是颗粒而不是文字、比例尺、背景纹理或其它非目标对象。
- 最终进入 V1 的标签必须转换成第 6 章通用格式：单通道 `uint16` PNG，`0=背景，1..N=不同颗粒实例`。
- 可视化叠加图、彩色 overlay、论文插图、截图和报告图不得直接作为标签；它们只能用于人工核对。
- 二值 mask、语义 mask、复用 id 标签、palette 图、polygon JSON 或其它历史格式必须显式转换为实例标签，并保留转换脚本或映射记录。
- 如果一个旧 id 对应多个不连通颗粒，不能把它当作一个实例；必须按官方转换逻辑或等价规则拆成独立实例。
- 连通分量自动拆分只能作为候选转换结果；粘连颗粒是否应拆开、碎片是否应保留，仍需人工抽样复核。
- 同一数据集来源、同一论文、同一实验批次、同一原始大图及其 tile 必须作为分组单元划分，不得跨 train/val/holdout 泄漏。
- 外部数据默认 `label_status` 不得直接填 `reviewed`；只有人工检查过边界、漏标、粘连拆分和标签语义后才能改为 `reviewed`。

合成数据可以用于补充训练分布，但只能进入 train，不得进入 val 或 holdout。合成数据必须在 manifest 的 `source` 或 `notes` 中明确记录，不得和真实显微图混淆。val 和 holdout 必须使用真实图片与真实人工复核标签。
