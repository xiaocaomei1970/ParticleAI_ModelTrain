# 外部数据源与泛化策略v1

本文档记录 V1 训练数据之外部来源评估、泛化风险、合成数据和不确定性策略。主流程执行仍以 [模型训练和C++后处理方案.md](模型训练和C++后处理方案.md) 为准；硬性数据格式与标注质量要求仍以 [数据准备手册.md](数据准备手册.md) 为准。

## 1. V1 泛化风险

材料/粉体领域的显微图分布非常宽：材料种类、粒径范围、形貌、分散方法、倍率、显微镜类型、对比度、噪声和背景纹理都会改变图像外观。V1 不应被定义成“所有材料/粉体都能一次性泛化”的通用模型。

有限样本训练得到的模型只能覆盖训练集中出现过的视觉分布。训练时没见过的材料不一定完全不能分割，但可靠性不能保证，常见失败包括漏分、粘连不拆、背景纹理误分、比例尺/文字误分和边界偏移。

数据准备时优先覆盖视觉场景，而不是按材料名称穷举：

- 形态：球形、椭圆、块状、不规则、片状、棒状、团聚。
- 密度：稀疏、中等、密集。
- 粘连：无粘连、少量接触、大量接触或堆叠。
- 质量：清晰、低对比、轻微模糊、噪声明显、背景纹理复杂。
- 尺度：小颗粒、大颗粒、大小混合、不同倍率。

V1 的合理定位是：先训练一个可用起点模型，再通过“新图推理 -> 人工修正 -> 回流训练”的闭环逐步扩展真实业务分布。

## 2. 外部数据源分级

外部数据源进入正式 V1 前必须先作为 candidate 处理，完成标签语义核验、格式转换和人工抽样复核。

| 等级 | 数据源 | 判断 | 用法 |
| --- | --- | --- | --- |
| A | Rettenberger / mlpmed | 材料/粉体 SEM，含标签；标签旧 id 可能复用，必须按官方 `clean_label()` 语义拆成实例。 | 可进入 V1 候选；复核后进入 train/val。 |
| A | EMPS | EM 颗粒实例分割数据，形貌较多样。 | 下载后筛材料/粉体、剔除截图/文字干扰，转换后人工复核。 |
| A/B | BAM TiO2 | TiO2 团聚颗粒，任务接近 V1；需检查 mask 是否为实例。 | 若为实例标签可直接候选；若为二值/语义 mask，需拆分并复核。 |
| B | Dryad HRTEM nanoparticles / CdSe 等 | HRTEM 纳米晶图，有标签，但晶格纹理明显、颗粒常截断。 | 只做辅助泛化或单独实验，不作为粉体 SEM 主数据。 |
| B | EMSeek nanoparticles | 多材料 EM 数据，可能有 mask/bbox，但标签语义需核验。 | 可用于辅助或人工复核后转实例。 |
| B/C | 2DFlakeSemSeg / OpenCrystalData | 形貌有参考价值，但不是标准粉体 SEM 实例数据。 | 只作为泛化补充或人工标注来源。 |
| C | NFFA-Europe SEM Powder/Particles | 大量 SEM 图片，无实例标签。 | 适合挑选图片后人工标注。 |
| D | 只有表格、论文截图、原子列定位、非颗粒实例任务的数据 | 与 V1 训练目标不匹配。 | 不进入 V1 数据源。 |

已确认不适合直接用于 V1 的例子：

- Mendeley `h7v36gfjhv`：下载后只有 `.xlsx`，无显微原图和实例标签。
- 原子列定位/原子级结构识别数据：任务目标不是颗粒实例轮廓。
- 不公开的工业数据：可记录为潜在申请对象，但不能作为当前训练来源。

## 3. 外部数据集接入规则

不要只根据文件名或目录名判断标签语义。每个外部数据集接入前必须完成以下检查：

1. 阅读数据集 README、论文 Data Availability、转换脚本或训练脚本。
2. 抽样打开原图、标签图、可视化叠加图，确认三者是否对齐。
3. 确认标签是否已经是“每个颗粒一个唯一实例 id”。
4. 如果标签是二值 mask、语义 mask、复用 id、polygon JSON 或彩色 palette 图，必须显式转换为 V1 通用格式。
5. 转换后必须输出单通道 `uint16` PNG：`0=背景，1..N=颗粒实例`。
6. 同一来源、同一论文、同一原始大图或同一图切出的 tile 必须分组划分，不得跨 train/val/holdout 泄漏。
7. 外部数据默认不得直接标 `reviewed`；必须人工抽样复核，确认边界、粘连拆分和漏标风险。

可视化叠加图，例如 `*_color.tif`、overlay、论文插图或截图，不得直接当作标签。它们只能用于人工检查；真正标签应来自原始 mask、polygon 或官方转换逻辑。

Rettenberger/mlpmed 的特殊规则：

- Low Mag 的 `Confident-Labels` 和 `Less-Certain_Labels` 目录语义正常。
- High Mag 原始目录名在官方脚本中有反向处理，必须以官方代码语义为准。
- `*_label.tif` 中同一个旧 id 可能对应多个不连通颗粒；必须按官方 `clean_label()` 逻辑或等价的“每个旧 id 内的每个连通分量拆成一个实例”转换。

## 4. 合成数据策略

参考 UTILE-Gen 的思路，可以从少量真实已标注图中提取单颗粒 ROI 和干净背景，生成新的 `image + instance mask` 对，用于扩展训练分布。

合成数据适合补充：

- 颗粒数量、位置、旋转、尺度变化。
- 稀疏/密集场景。
- 背景灰度、噪声、轻微模糊和对比度变化。
- 不同形貌颗粒组合。

合成数据限制：

- 不能替代真实显微图。
- 不能用于 val 或 holdout。
- 不能制造真实显微物理中不存在的结构。
- 不能掩盖真实数据标注不足的问题。

V1 使用建议：

- 合成数据只进入 train。
- val/holdout 必须全部使用真实图片。
- manifest 中必须标记来源，例如 `source=synthetic`，并在 `notes` 中记录生成规则。
- 初期合成数据比例不宜过高，必须以真实 holdout 指标判断是否有效。

## 5. 不确定性与人工复核

BPartIS 的贝叶斯颗粒实例分割给 V1/V2 的主要启发是：真实应用中不应只输出 mask，还应输出每个颗粒结果是否可信。

V1 阶段不强制实现贝叶斯模型，但应保留扩展方向：

- 使用 test-time augmentation 或 MC dropout 进行多次推理。
- 统计同一颗粒在多次推理中的存在性、面积、边界和拆分/合并稳定性。
- 输出颗粒级 `confidence` 或 `uncertainty`。
- 将低置信颗粒送入人工复核队列，而不是直接进入粒度统计。

这类不确定性评估属于运行时质量控制和 V2 能力，不替代 V1 的数据准备、标签复核和 holdout 验收。

## 6. 数据闭环

V1 建议采用持续迭代闭环：

1. 用真实已复核样本训练起点模型。
2. 对新材料/新分散方法/新倍率图片做推理。
3. 人工修正漏分、误分和粘连拆分。
4. 将修正后的样本作为新批次回流。
5. 重新分层划分并保留独立 holdout。
6. 只在真实 holdout 提升时接受新模型。

对从未见过的材料或形貌，不应直接相信模型结果。建议先标注 5-20 张代表图做小样本验证或增量训练，再进入正式粒度分析流程。
